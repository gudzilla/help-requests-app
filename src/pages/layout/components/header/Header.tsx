import { Box } from '@mui/material';
import { ResponsiveAppBar } from './ResponsiveAppBar';

export const Header = () => {
  return (
    <Box component="header">
      <ResponsiveAppBar />
    </Box>
  );
};
